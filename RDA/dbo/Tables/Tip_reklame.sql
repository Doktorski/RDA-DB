CREATE TABLE [dbo].[Tip_reklame] (
    [Tip_reklame_ID] INT           IDENTITY (1, 1) NOT NULL,
    [Naziv]          NVARCHAR (15) NOT NULL,
    PRIMARY KEY CLUSTERED ([Tip_reklame_ID] ASC)
);

